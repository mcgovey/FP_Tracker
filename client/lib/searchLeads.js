  Template.searchLead.helpers({
    updateDoc: function () {
      // console.log('this is running');
      
      // Leads.find().forEach(function(doc){
      // 	console.log(doc._id);
      // 	Leads.update({_id:doc._id},{$set:{_id:doc._id._str}});
      // 	console.log(doc._id);
      // });
      
      // var test = Leads.findOne({_id: Mongo.ObjectID("56670f7c3b9b7fa8a8e310a8")});
      // console.log('foundTest',test);
    }
  });